name: Build and Deploy Hugo Site

on:
  push:
    branches:
      - main
    paths:
      - "content/**/*.md"
      - "static/**"
      - "layouts/**"
      - "assets/**"
  workflow_dispatch: # 支持手动触发

concurrency:
  group: deploy-pages
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout source repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.PERSONAL_TOKEN }} # 用于图片上传需要推送权限

      - name: Git Configuration
        run: |
          git config --global core.quotePath false
          git config --global core.autocrlf false
          git config --global core.safecrlf true
          git config --global core.ignorecase false

      # ============ 迁移 CSDN 图片 ============
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Install Python dependencies
        run: |
          pip install requests

      - name: Migrate CSDN images
        env:
          PERSONAL_TOKEN: ${{ secrets.PERSONAL_TOKEN }}
        run: |
          python migrate_csdn_images.py

      # ============ 👇 请完整复制并覆盖这一步 👇 ============
      - name: Commit and Push Markdown changes
        run: |
          # 1. 配置用户
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
          # 2. 先检查本地是否有 Python 脚本修改过的文件
          if [[ -n $(git status --porcelain content/) ]]; then
            echo "检测到 Markdown 文件链接已更新，先提交本地修改..."
            
            # --- 关键改变：先 Commit，锁住修改 ---
            git add content/
            git commit -m "Auto update markdown links (CSDN migration) [skip ci]"
            
            echo "本地已提交，现在拉取远程图片的变动..."
            
            # --- 关键改变：Commit 之后再 Pull ---
            # 这样 Git 就知道如何合并了（Rebase 策略）
            git pull origin main --rebase
            
            # 3. 最后一起推送
            git push origin main
            echo "✅ 成功：Markdown 更新已推送到仓库。"
            
          else
            echo "Markdown 文件没有变化。"
            # 即使没变化，为了保险起见，也可以拉取一下最新代码，但不强求
            # 如果这里没有任何提交，不 pull 也行，或者为了后续步骤稳妥：
            git pull origin main --rebase
          fi

      # ============ 构建 Hugo 站点 ============
      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v3
        with:
          hugo-version: "latest"
          extended: true

      - name: Build site
        run: hugo -D # 包含 draft 文章

      # ============ 部署到 GitHub Pages ============
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          personal_token: ${{ secrets.PERSONAL_TOKEN }}
          external_repository: iamxurulin/iamxurulin.github.io
          publish_branch: main
          publish_dir: ./public
          force_orphan: true
          # git_push_flags: '--force'  <-- 删除了这行无效配置，警告会消失
          commit_message: "auto deploy: ${{ github.event.head_commit.message }}"
          user_name: "github-actions[bot]"
          user_email: "github-actions[bot]@users.noreply.github.com"
