---
title: "二分法-二维数组中的查找"
date: 2026-01-10 17:29:11
categories:
- 算法
tags:
  - 数组
draft: false
---

![在这里插入图片描述](https://i-blog.csdnimg.cn/direct/0b824c24510445e3b2d6ecd3e2ff275e.jpeg#pic_center)
### 求解代码

这道题因为每一行从左到右都是递增的，且每一列从上到下也都是递增的。

我们不难发现，左上角是**最小值**，右下角是**最大值**。

左下的元素大于它上方的元素，小于它右方的元素。

所以，我们可以以左下角为起点，如果它小于target，则往右移找更大的；

如果它大于target，则往上移找更小的。


```java
public boolean Find (int target, int[][] array) {

		if(array.length==0||array[0].length==0){
			return false;
		}

		int n = array.length;
		int m = array[0].length;

		for(int i=n-1,j=0;i>=0&&j<m;){
			if(array[i][j]<target){
				j++;
			}else if(array[i][j]==target){
				return true;
			}else{
				i--;
			}
		}
		return false;
    }
```

