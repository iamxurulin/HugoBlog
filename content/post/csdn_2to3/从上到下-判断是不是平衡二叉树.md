---
title: "【从上到下】判断是不是平衡二叉树"
date: 2026-01-13 20:52:09
categories:
- 算法
tags:
- 算法
draft: false
---

![在这里插入图片描述](https://i-blog.csdnimg.cn/direct/68b714e0c0ec461f868c70c75732763c.jpeg#pic_center)
![在这里插入图片描述](https://i-blog.csdnimg.cn/direct/e6bd6e9411654cbc8139f5f5a035e5c6.jpeg#pic_center)
### 求解代码
平衡二叉树的核心判断条件：

 - 当前节点的左右子树高度差小于等于1；
   
 -  当前节点的左右子树本身也是平衡二叉树；

```java
  public boolean IsBalanced_Solution (TreeNode pRoot) {
       if(pRoot == null){
        return true;
       }

       int left = depth(pRoot.left);
       int right = depth(pRoot.right);

       return Math.abs(left-right)<=1&&IsBalanced_Solution(pRoot.left)&&IsBalanced_Solution(pRoot.right);
}

    public int depth(TreeNode root) {
        if(root == null){
            return 0;
        }

        return Math.max(depth(root.left),depth(root.right))+1;
    }
```

