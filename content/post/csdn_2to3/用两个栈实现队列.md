---
title: "用两个栈实现队列"
date: 2026-01-15 00:14:18
categories:
- 算法
tags:
  - 栈与队列
draft: false
---

![在这里插入图片描述](/images/85009961dade49f2acefdd0b450e27aa-f3ef9571.jpeg)
### 求解代码

```java
Stack<Integer> stack1 = new Stack<Integer>();
    Stack<Integer> stack2 = new Stack<Integer>();
    
    public void push(int node) {
        stack1.push(node);
    }
    
    public int pop() {
        if(stack2.isEmpty()){
            while (!stack1.isEmpty()) {
                stack2.push(stack1.pop());
            }
        } 
        return stack2.pop();
    }
```
### 小贴士
- 所有入队的元素，无条件直接压入 `stack1`
- `stack1` 只管入队，`stack2` 只管出队，并且只有当`stack2` 为空时才进行一次性**倒栈**
