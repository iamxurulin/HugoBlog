---
title: "链表中环的入口结点"
date: 2026-01-07 23:07:19
categories:
- 算法
tags:
  - 链表
draft: false
---

![在这里插入图片描述](https://i-blog.csdnimg.cn/direct/47a92f2a68c1475b89ccc159acd25835.jpeg#pic_center)
![在这里插入图片描述](https://i-blog.csdnimg.cn/direct/a117cbdc18f04e028ea7514e42ed17f5.jpeg#pic_center)
### 求解代码

```java
public ListNode EntryNodeOfLoop(ListNode pHead) {
        // 快慢指针都从链表头出发
        ListNode fast = pHead;
        ListNode slow = pHead;


        while (fast != null && fast.next != null) {
            fast = fast.next.next; // 快指针走2步
            slow = slow.next;      // 慢指针走1步

            // 快慢指针相遇 → 证明链表有环，进入找入口逻辑
            if (slow == fast) {
                // 找环的入口
                while (pHead != slow) {
                    pHead = pHead.next; // 头指针走1步
                    slow = slow.next; // 相遇点指针走1步
                }
                return slow; // 相遇时就是环的入口
            }
        }
				// 无环，返回null
        return null;
    }
```

