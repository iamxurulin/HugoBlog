---
title: "链表内指定区间反转"
date: 2026-01-06 22:40:21
categories:
- 算法
tags:
- leetcode
draft: false
---

![在这里插入图片描述](https://i-blog.csdnimg.cn/direct/a427640dfcc84852a1b65e1a709ebd3c.jpeg#pic_center)


### 代码求解
reverseN函数是实现将链表的前n个节点进行反转。

和反转整个链表不同，reverseN是要让反转之后的head节点和后面的节点连接起来，而不是指向null。

因此，head.next需要指向successor。

```java
    ListNode successor = null;
    public ListNode reverseBetween (ListNode head, int m, int n) {
        if(m==1){
            return reverseN(head,n);
        }
        head.next=reverseBetween(head.next,m-1,n-1);
        return head;
    }
    public ListNode reverseN(ListNode head,int n){
        if(n==1){
            successor = head.next;
            return head;
        }
        ListNode last = reverseN(head.next,n-1);
        head.next.next = head;
        head.next = successor;
        return last;
    }
```

